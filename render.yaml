services:
  - type: web
    name: donation-manager
    env: node
    buildCommand: echo "Skipping build to save pipeline minutes"
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
    autoDeploy: false